import styled from 'styled-components';

const color = '#3F51B5';

export const PasswordChangeLinkStyled = styled.p`
    display: inline;
    font-size: 1rem;
    color: ${color};
    cursor: pointer;
`;